-- ============================================================
-- Миграция 019: период абонемента у платежей (для продления и архива)
-- Выполнить после 001. Идемпотентна.
-- ============================================================

ALTER TABLE payments
  ADD COLUMN IF NOT EXISTS period_start DATE,
  ADD COLUMN IF NOT EXISTS period_end DATE;
